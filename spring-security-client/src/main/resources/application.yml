server:
  port: 8083
---
spring:
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: 'true'
  datasource:
    url: "jdbc:postgresql://localhost:5432/user_registration_db"
    username: postgres
    password: root
